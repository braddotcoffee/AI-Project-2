

class Hands():

    def __init__(self):

    def main():
        #loop:
            #wait several ms
            #poll for move_file
            #if exists:
                #init all the things
        #loop:
            #wait several ms
            #poll for our file
            #if exists:
                #read move_file
                #if we don't know what color we are:
                    #if empty:
                        #we are first to move
                    #else
                        #we're second to move
                #pass last move to body

    def write_move(move):
        #open move_file
        #append in our move
        #close file
